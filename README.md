# 🗺️ 여행 플래너 (Travel Planner)

Vue 3 + Vite + Leaflet.js 기반의 **지도 중심 여행 개인화 웹페이지**

## ✨ 주요 기능

- 📍 **대화형 지도**: Leaflet.js + OpenStreetMap (API 키 불필요!)
- 🚆 **교통편 검색**: 비행기, 기차, 버스 옵션 비교
- 💰 **스마트 정렬**: 최저가, 최단시간 정렬
- 🎫 **취소표 필터링**: 취소 가능한 티켓만 보기
- 📅 **자동 일정 생성**: 날짜별 상세 일정표
- 🎨 **취향 분석**: AI 기반 여행지 추천 (확장 가능)
- 🗺️ **경로 시각화**: 지도에 이동 경로 및 마커 표시
- 🔄 **양방향 연동**: 일정표 ↔ 지도 상호작용

## 🚀 로컬 실행 방법

### 1. 의존성 설치
```bash
npm install
```

### 2. 개발 서버 실행
```bash
npm run dev
```

브라우저에서 `http://localhost:5173` 접속

### 3. 프로덕션 빌드
```bash
npm run build
```

### 4. 빌드 미리보기
```bash
npm run preview
```

## 📦 GitHub Pages 배포

### 자동 배포 (gh-pages 사용)

```bash
npm run deploy
```

이 명령어는 다음을 수행합니다:
1. `npm run build` - 프로젝트 빌드
2. `gh-pages -d dist` - dist 폴더를 gh-pages 브랜치에 배포

### 배포 후 확인

배포 완료 후 다음 URL에서 확인 가능:
```
https://<username>.github.io/<repository-name>/
```

예: `https://yourusername.github.io/solideo-Day2-01-21-Practice2/`

### GitHub Pages 설정

1. GitHub 저장소 → Settings → Pages
2. Source: `gh-pages` 브랜치 선택
3. Save 클릭

## 📁 프로젝트 구조

```
travel-planner/
├── src/
│   ├── components/          # Vue 컴포넌트
│   │   ├── MapComponent.vue      # 🗺️ Leaflet 지도 (핵심!)
│   │   ├── SearchForm.vue        # 검색 폼
│   │   ├── TransportList.vue     # 교통편 목록
│   │   └── ScheduleTable.vue     # 일정표
│   ├── pages/               # 페이지
│   │   ├── HomePage.vue          # 홈 페이지
│   │   └── ResultPage.vue        # 결과 페이지
│   ├── data/                # 데이터
│   │   ├── types.ts              # TypeScript 타입 정의
│   │   ├── mockTransports.ts     # 더미 교통편 데이터
│   │   └── mockPlaces.ts         # 더미 장소 데이터
│   ├── logic/               # 비즈니스 로직
│   │   ├── schedulePlanner.ts    # 일정 자동 생성
│   │   └── preferenceParser.ts   # 취향 분석 (AI 확장 가능)
│   ├── router/              # Vue Router 설정
│   │   └── index.ts
│   ├── App.vue              # 루트 컴포넌트
│   └── main.ts              # 앱 진입점
├── public/
│   └── .nojekyll           # GitHub Pages Jekyll 비활성화
├── vite.config.ts          # Vite 설정 (base 경로 포함)
└── package.json
```

## 🛠️ 기술 스택

- **프레임워크**: Vue 3 (Composition API)
- **빌드 도구**: Vite
- **라우팅**: Vue Router 4
- **지도**: Leaflet.js 1.9+ (OpenStreetMap)
- **날짜 처리**: date-fns
- **언어**: TypeScript
- **배포**: GitHub Pages

## 🎯 사용 방법

### 1. 여행 정보 입력 (홈 페이지)
- 출발지/도착지 선택
- 여행 기간 및 출발 시간 설정
- 교통수단 선택 (복수 선택 가능)
- 예산 입력 (선택)
- 취향 텍스트 작성 (예: "카페 좋아함, 조용한 곳 선호")

### 2. 결과 확인 (결과 페이지)
- **좌측**: 교통편 목록 + 일정표
  - 정렬/필터 기능 사용
  - 원하는 교통편 클릭하여 선택
- **우측**: 대형 지도
  - 경로 및 마커 자동 표시
  - 마커 클릭 → 일정표 하이라이트
  - 일정표 클릭 → 지도 이동

## 🔧 확장 가능성

### AI 취향 분석 연동
`src/logic/preferenceParser.ts`의 `callAiPreference()` 함수를 실제 LLM API로 교체:

```typescript
export async function callAiPreference(text: string): Promise<string[]> {
  const response = await fetch('https://api.openai.com/v1/chat/completions', {
    // OpenAI, Claude, Gemini 등 API 연동
  });
  // ...
}
```

### 실제 교통 API 연동
`src/data/mockTransports.ts`를 실제 API 호출로 교체

### 데이터베이스 연동
여행 일정 저장 및 공유 기능 추가

## 📝 주요 특징

### ✅ API 키 불필요
Leaflet.js + OpenStreetMap을 사용하여 **API 키 없이** 지도 기능 구현

### ✅ 완전한 반응형
Vue 3의 `reactive`와 `computed`를 활용한 실시간 업데이트

### ✅ 양방향 연동
지도 ↔ 일정표 간 완벽한 상호작용

### ✅ GitHub Pages 최적화
- Hash 모드 라우팅 (`createWebHashHistory`)
- Base 경로 자동 설정
- `.nojekyll` 파일 포함

## 🤝 기여

이슈 및 풀 리퀘스트 환영합니다!

## 📄 라이선스

MIT License - LICENSE.md 참조
